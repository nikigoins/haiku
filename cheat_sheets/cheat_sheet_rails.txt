bundle
  *Update/install software (from the Gemfile)

rake db:migrate
  *Update your database

rails s -b $IP -p $PORT
  Turns on the site.  Will only work on could9.
  `rails server` for local Mac machines

rails console

control c
  Turns off the site
  
**Delete your database and create a new one
rake db:drop
rake db:create
rake db:migrate
rake db:seed

*** Open a rails terminal
*** that is connected to your database
rails console
